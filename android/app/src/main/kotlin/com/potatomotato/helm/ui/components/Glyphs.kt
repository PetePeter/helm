package com.potatomotato.helm.ui.components

/**
 * The row-opens-a-screen marker, said once.
 *
 * Three list screens each declared their own private copy, which is three
 * chances for the plan board, the lane list and the context browser to end up
 * pointing at their detail screens with three different characters.
 */
internal const val CHEVRON = "›"
